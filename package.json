{
  "name": "@magic-bridge/root",
  "scripts": {
    "start": "node lib/spec/test-server",
    "start-bg": "nohup node lib/spec/test-server.js &",
    "stop" : "pkill magic-bridge-test-server",
    "pretest" : "npm run start-bg",
    "test": "mocha 'lib/**/*.spec.js'",
    "posttest" : "npm stop"
  },
  "devDependencies": {
    "parcel": "^2.6.0",
    "mocha": "^10.0.0",
    "express": "^4.18.1",
    "express-session": "^1.17.3"
  }
}
