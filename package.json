{
  "name": "magic-bridge",
  "scripts": {
    "start": "node spec/test-server",
    "stop" : "pkill --signal SIGINT magic-bridge-test-server",
    "test": "mocha spec/e2e.spec.js"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "express": "^4.18.1",
    "express-session": "^1.17.3"
  }
}
